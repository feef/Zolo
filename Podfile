# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Zolo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Zolo
  pod 'SwiftyJSON'
  pod 'SDWebImage'

  target 'ZoloTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ZoloUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
